/**
 * Here we will see how aggregate functions will work
 * in case we don't give any values to them
 */

 // while checking max it starts with comparing values with -infinity
 // so if value then -infinity will be max value
 console.log(Math.max()) // ??? It will output -infinity

 // While checking min it starts with comparing values with infinity
 // so if no value then infinity will be min value
 console.log(Math.min()) // ??? It will outpu infinity